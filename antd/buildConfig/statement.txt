该目录可以视为一个小型的 node 项目，包含两个功能
1.通过一些规则生成一个抽象配置文件（描述文件） _buildConf.js
2.使用由配置文件（描述文件）生成的配置文件生成项目 _config.js
****** _buildConf.js 是提供给一个可视化界面的，用于生成可视化的配置使用的
********* 例如描述 布局 有几种情况，默认为哪一种，布局的大致内容需要哪些配置信息
****** _config.js 是由可视化界面生成的，用于生成项目的
********* 例如描述 布局 为 布局1，参数内容为 {...}

/**
 * 1 这个文件应当是对项目的抽象表示，
 *      可以使用它生成一个项目配置并通过配置生产文件
 * x 这个文件应该是被序列化并能被反序列化的
 *
 * 这个描述文件本身应该包含一个项目初始化时的步骤描述
 * */
_buildConf.js

utils